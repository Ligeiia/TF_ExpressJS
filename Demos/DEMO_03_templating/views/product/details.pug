extends ../layout

block main 
    .profile 
        h1= title 
        .card 
            img(src=`/images/${product.image}`)
            h2= product.nom 
            p.description= product.description
            p.prix= product.prix
            p.disponibilite= product.disponibilite.message 
        a.button(href="/products") Back
